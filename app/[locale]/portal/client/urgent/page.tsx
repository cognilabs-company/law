import UrgentAdvocatePanel from "@/components/portal/UrgentAdvocatePanel";

// "Tezkor Advokat xizmati online" — the whole module on one page: the four
// services, the order form and the client's own requests
// (LEXGO_URGENT_ADVOCATE_FRONTEND_UPDATE.md).
export default function Page() {
  return <UrgentAdvocatePanel />;
}
