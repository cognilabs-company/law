diff --git a/ai_service.py b/ai_service.py
index 2501209..bcbcc94 100644
--- a/ai_service.py
+++ b/ai_service.py
@@ -32,19 +32,113 @@ Hech qachon yakuniy yuridik maslahat advokat tekshiruvini almashtiradi deb ko'rs
 """
 
 
+# FIX (T0-17, 2026-09-18): this table only ever covered phone / PINFL /
+# passport / email. GM v1.1 requirement #1 also lists F.I.Sh. (name), manzil
+# (address) and bank rekvizitlari (card number) — none of those were masked,
+# so a message like "Mening ismim Aziz Karimov ... kartam 8600 1234 5678
+# 9012 ..." went to the external AI with the name and card number in plain
+# text. Bank card + a best-effort address heuristic added below; the name is
+# handled separately via `known_terms` (see PiiVault) since a name has no
+# fixed pattern — we mask the exact string(s) we already know from the
+# caller's own account (name / first / last / middle name).
+#
+# Each entry is (pattern, kind) — `kind` becomes part of a *reversible*
+# token (see PiiVault below), not a fixed "[X_REDACTED]" string, so the
+# original value can be restored in the AI's reply (requirement #2, which
+# had no implementation at all before this fix: a reply that echoed back a
+# masked span showed the literal "[PHONE_REDACTED]" placeholder to the user).
 PII_PATTERNS = (
-    (re.compile(r"(?<!\d)\+?998[\s().-]?\d{2}[\s().-]?\d{3}[\s.-]?\d{2}[\s.-]?\d{2}(?!\d)"), "[PHONE_REDACTED]"),
-    (re.compile(r"\b\d{14}\b"), "[PINFL_REDACTED]"),
-    (re.compile(r"\b[A-Z]{2}\s?\d{7}\b", re.IGNORECASE), "[PASSPORT_REDACTED]"),
-    (re.compile(r"\b[\w.+-]+@[\w.-]+\.[A-Za-z]{2,}\b"), "[EMAIL_REDACTED]"),
+    (re.compile(r"(?<!\d)\+?998[\s().-]?\d{2}[\s().-]?\d{3}[\s.-]?\d{2}[\s.-]?\d{2}(?!\d)"), "PHONE"),
+    (re.compile(r"\b\d{14}\b"), "PINFL"),
+    (re.compile(r"\b[A-Z]{2}\s?\d{7}\b", re.IGNORECASE), "PASSPORT"),
+    (re.compile(r"\b[\w.+-]+@[\w.-]+\.[A-Za-z]{2,}\b"), "EMAIL"),
+    # Uzcard / Humo / Visa / Mastercard — 16 digits, optionally grouped in 4s
+    # with spaces or dashes (matches "8600 1234 5678 9012").
+    (re.compile(r"\b\d{4}[\s.-]?\d{4}[\s.-]?\d{4}[\s.-]?\d{4}\b"), "BANK"),
+    # Best-effort address heuristic: up to 2 words right before an address
+    # keyword (shahar/tuman/ko'cha/mahalla/kvartal/uy/mavze), the keyword
+    # itself, and the rest of that clause. Not exhaustive — a proper NER pass
+    # would also catch addresses with no keyword — but it is a large
+    # improvement over masking nothing at all. (No trailing \b after the
+    # keyword group: "sh\." ends on a literal ".", and "." next to the space
+    # that usually follows it is never a \b, so a trailing \b would silently
+    # stop this alternative from ever matching "Toshkent sh." — verified.)
+    (
+        re.compile(
+            r"(?:\S+\s+){0,2}\b(?:shahar|sh\.|tuman|ko'cha|kocha|mahalla|mavze|kvartal|uy)[^,.\n]{0,60}",
+            re.IGNORECASE,
+        ),
+        "ADDRESS",
+    ),
 )
 
 
-def mask_pii(value: str) -> str:
-    masked = value
-    for pattern, replacement in PII_PATTERNS:
-        masked = pattern.sub(replacement, masked)
-    return masked
+class PiiVault:
+    """Collects the PII spans found while masking outbound text for one AI
+    turn, each behind a unique, reversible token — so `unmask()` can restore
+    the real values in the AI's reply afterwards. Create one per request/turn
+    (`run_ai`, `run_seller_ai`, …); never share or persist it, the tokens are
+    only meaningful within that single round trip."""
+
+    def __init__(self) -> None:
+        self._by_token: dict[str, str] = {}
+        self._by_value: dict[str, str] = {}
+        self._next = 1
+
+    def _token_for(self, original: str, kind: str) -> str:
+        existing = self._by_value.get(original.lower())
+        if existing:
+            return existing
+        token = f"[{kind}_{self._next}]"
+        self._next += 1
+        self._by_token[token] = original
+        self._by_value[original.lower()] = token
+        return token
+
+    def mask(self, value: str, known_terms: list[str] | None = None) -> str:
+        if not value:
+            return value
+        masked = value
+        # Longest known term first, so "Aziz Karimov" is masked whole before
+        # a bare "Aziz" inside it could match on its own.
+        for term in sorted({t.strip() for t in (known_terms or []) if t and len(t.strip()) >= 3}, key=len, reverse=True):
+            masked = re.sub(re.escape(term), lambda m, t=term: self._token_for(t, "NAME"), masked, flags=re.IGNORECASE)
+        for pattern, kind in PII_PATTERNS:
+            masked = pattern.sub(lambda m, k=kind: self._token_for(m.group(0), k), masked)
+        return masked
+
+    def unmask(self, value: str) -> str:
+        if not value:
+            return value
+        restored = value
+        for token, original in self._by_token.items():
+            restored = restored.replace(token, original)
+        return restored
+
+
+def name_terms_for(user) -> list[str]:
+    """The known-name strings for an authenticated user (None for a guest),
+    longest first — exactly what PiiVault.mask needs for the NAME pass."""
+    if not user:
+        return []
+    full = " ".join(filter(None, [getattr(user, "first_name", None), getattr(user, "last_name", None), getattr(user, "middle_name", None)])).strip()
+    candidates = [getattr(user, "name", None), full, getattr(user, "first_name", None), getattr(user, "last_name", None), getattr(user, "middle_name", None)]
+    seen: set[str] = set()
+    terms: list[str] = []
+    for c in candidates:
+        c = (c or "").strip()
+        if len(c) >= 3 and c.lower() not in seen:
+            seen.add(c.lower())
+            terms.append(c)
+    return terms
+
+
+# Back-compat: a one-off mask with no restore (fine for a fire-and-forget
+# audit-log line, e.g. add_activity(...); never use this where the masked
+# text is sent to the AI and the reply is shown to the user — use a
+# PiiVault there so the reply can be unmasked).
+def mask_pii(value: str, known_terms: list[str] | None = None) -> str:
+    return PiiVault().mask(value, known_terms)
 
 
 TOOLS = [
@@ -78,12 +172,12 @@ TOOLS = [
 ]
 
 
-def build_input(chat: Chat, client_message: str) -> list[dict[str, str]]:
+def build_input(chat: Chat, client_message: str, vault: "PiiVault", known_terms: list[str] | None = None) -> list[dict[str, str]]:
     items = []
     for message in chat.messages[-30:]:
         if message.role.value in {"user", "assistant"}:
-            items.append({"role": message.role.value, "content": mask_pii(message.content)})
-    items.append({"role": "user", "content": mask_pii(client_message)})
+            items.append({"role": message.role.value, "content": vault.mask(message.content, known_terms)})
+    items.append({"role": "user", "content": vault.mask(client_message, known_terms)})
     return items
 
 
@@ -168,39 +262,45 @@ def call_tool(db: Session, chat_id: str, name: str, arguments: dict[str, Any]) -
         return {"error": str(error), "tool": name}
 
 
-def build_final_prompt(client_message: str, tool_results: list[dict[str, Any]], contracts: list[dict[str, str]]) -> str:
+def build_final_prompt(client_message: str, tool_results: list[dict[str, Any]], contracts: list[dict[str, str]], vault: "PiiVault", known_terms: list[str] | None = None) -> str:
     return (
         "Tool chaqirishni to'xtating va hozir final javob yozing. "
         "Yangi function call chaqirmang. "
         "Mijoz savoliga mavjud rasmiy manba natijalari asosida aniq, amaliy va qisqa javob bering. "
         "Agar manbalarda yetarli ma'lumot bo'lmasa, buni ayting va umumiy yo'l-yo'riq bering. "
         "Agar PDF contract yaratilgan bo'lsa, link yozmang, faqat PDF fayl tayyorligini ayting. "
-        f"Mijoz xabari: {mask_pii(client_message)}\n"
-        f"Tool natijalari: {mask_pii(json.dumps(tool_results, ensure_ascii=False))[:16000]}\n"
+        f"Mijoz xabari: {vault.mask(client_message, known_terms)}\n"
+        f"Tool natijalari: {vault.mask(json.dumps(tool_results, ensure_ascii=False), known_terms)[:16000]}\n"
         f"Yaratilgan PDF fayllar: {json.dumps(contracts, ensure_ascii=False)[:4000]}"
     )
 
 
-def generate_final_answer(client: OpenAI, model: str, client_message: str, tool_results: list[dict[str, Any]], contracts: list[dict[str, str]]) -> str:
+def generate_final_answer(client: OpenAI, model: str, client_message: str, tool_results: list[dict[str, Any]], contracts: list[dict[str, str]], vault: "PiiVault", known_terms: list[str] | None = None) -> str:
     response = client.responses.create(
         model=model,
         instructions=SYSTEM_PROMPT,
-        input=[{"role": "user", "content": build_final_prompt(client_message, tool_results, contracts)}],
+        input=[{"role": "user", "content": build_final_prompt(client_message, tool_results, contracts, vault, known_terms)}],
     )
-    return get_output_text(response)
+    # Restored here (not by the caller) so every path that can produce the
+    # final answer text goes through the same unmask step.
+    return vault.unmask(get_output_text(response))
 
 
-def run_ai(db: Session, chat: Chat, client_message: str) -> tuple[str, list[dict[str, str]], list[dict[str, str]]]:
+def run_ai(db: Session, chat: Chat, client_message: str, known_terms: list[str] | None = None) -> tuple[str, list[dict[str, str]], list[dict[str, str]]]:
     settings = get_settings()
     if not settings.openai_api_key:
         return "OPENAI_API_KEY sozlanmagan. `.env` faylga API key qo'shing.", [], []
     client = OpenAI(api_key=settings.openai_api_key)
     tool_results = []
+    # One vault per turn: every mask() call below (this message, the chat
+    # history, the tool arguments, the final-answer fallback prompt) shares
+    # it, so a single unmask() at the end restores everything consistently.
+    vault = PiiVault()
     try:
         response = client.responses.create(
             model=settings.openai_model,
             instructions=SYSTEM_PROMPT,
-            input=build_input(chat, client_message),
+            input=build_input(chat, client_message, vault, known_terms),
             tools=TOOLS,
         )
         answer = get_output_text(response)
@@ -218,7 +318,7 @@ def run_ai(db: Session, chat: Chat, client_message: str) -> tuple[str, list[dict
                         result = {"error": "Search limit reached. Use existing source results and write final answer."}
                     else:
                         arguments = json.loads(getattr(call, "arguments", "{}") or "{}")
-                        arguments["query"] = mask_pii(arguments.get("query", ""))
+                        arguments["query"] = vault.mask(arguments.get("query", ""), known_terms)
                         result = call_tool(db, chat.id, call.name, arguments)
                 elif call.name == "fetch_legal_source":
                     fetch_calls += 1
@@ -250,7 +350,12 @@ def run_ai(db: Session, chat: Chat, client_message: str) -> tuple[str, list[dict
                 break
         contracts = extract_contracts_from_tools(tool_results)
         if not answer:
-            answer = generate_final_answer(client, settings.openai_model, client_message, tool_results, contracts)
+            answer = generate_final_answer(client, settings.openai_model, client_message, tool_results, contracts, vault, known_terms)
+        else:
+            # generate_final_answer restores its own output; the two other
+            # sources of `answer` above (the direct reply, the post-tool-call
+            # reply) are still in raw masked-token form until here.
+            answer = vault.unmask(answer)
     except OpenAIError as error:
         return f"AI servis bilan aloqa vaqtida xatolik bo'ldi: {error}", extract_sources_from_tools(tool_results), extract_contracts_from_tools(tool_results)
     except Exception as error:
diff --git a/main.py b/main.py
index ad77e95..03f102d 100644
--- a/main.py
+++ b/main.py
@@ -21,7 +21,7 @@ from sqlalchemy import desc, func, select
 from sqlalchemy.exc import IntegrityError
 from sqlalchemy.orm import Session
 
-from ai_service import mask_pii, run_ai
+from ai_service import mask_pii, name_terms_for, run_ai
 from auth_service import consume_guest_ai_limit, create_access_token, create_refresh_session, demo_otp_code, get_current_user, get_optional_current_user, get_optional_websocket_user, get_refresh_session, get_request_ip, get_user_by_phone, get_websocket_ip, hash_otp, hash_password, lexgo_id_for, normalize_phone, seed_permissions_and_roles, user_permission_codes, verify_otp, verify_password
 from catalog_seed import seed_catalog
 from config import get_settings
@@ -1317,7 +1317,7 @@ async def send_message(client_id: str, chat_id: str, payload: MessageCreate, req
     db.commit()
     db.refresh(user_message)
     await manager.broadcast(chat.id, {"event": "message.created", "message": message_to_out(user_message).model_dump(mode="json")})
-    answer, sources, contracts = run_ai(db, chat, payload.content)
+    answer, sources, contracts = run_ai(db, chat, payload.content, name_terms_for(user))
     assistant_message = Message(
         chat_id=chat.id,
         role=MessageRole.assistant,
@@ -1414,7 +1414,7 @@ async def chat_websocket(websocket: WebSocket, client_id: str, chat_id: str) ->
                 db.commit()
                 db.refresh(user_message)
                 await manager.broadcast(chat.id, {"event": "message.created", "message": message_to_out(user_message).model_dump(mode="json")})
-                answer, sources, contracts = run_ai(db, chat, content)
+                answer, sources, contracts = run_ai(db, chat, content, name_terms_for(user))
                 assistant_message = Message(
                     chat_id=chat.id,
                     role=MessageRole.assistant,
diff --git a/marketplace_routes.py b/marketplace_routes.py
index 90f0347..a3b14a8 100644
--- a/marketplace_routes.py
+++ b/marketplace_routes.py
@@ -19,7 +19,7 @@ from fastapi.responses import FileResponse, StreamingResponse
 from fastapi.routing import APIRoute
 from openai import OpenAI, OpenAIError
 from pydantic import BaseModel, Field
-from ai_service import mask_pii
+from ai_service import PiiVault, mask_pii, name_terms_for
 from business_calendar import add_business_minutes, business_hours_status as calendar_status
 from sqlalchemy import delete, func, select
 from sqlalchemy.orm import Session, selectinload
@@ -655,18 +655,23 @@ def ai_fallback_answer(prompt: str, context_text: str) -> str:
     return f"Qisqa xulosa: {short or 'Tahlil uchun matn yetarli emas.'}\n\nAsosiy holatlar, tomonlar, muddatlar va talab qilinayotgan natijani alohida tekshirish tavsiya etiladi."
 
 
-def run_seller_ai(prompt: str, context_text: str, task: str) -> str:
+def run_seller_ai(prompt: str, context_text: str, task: str, known_terms: list[str] | None = None) -> str:
     settings = get_settings()
     if not settings.openai_api_key:
         return ai_fallback_answer(prompt, context_text)
+    # One vault for this call: mask() below, unmask() on the way out, so a
+    # reply that echoes a masked span (e.g. the caller's own name) shows the
+    # real value again instead of the literal "[NAME_1]" placeholder.
+    vault = PiiVault()
     try:
         client = OpenAI(api_key=settings.openai_api_key)
         response = client.responses.create(
             model=settings.openai_model,
             instructions="Siz LexGo seller assistant modulida ishlaysiz. O'zbek tilida, amaliy, aniq va markdown formatda javob bering. Ichki model/provayder haqida gapirmang. Huquqiy yakuniy xulosa o'rnini advokat tekshiruvi bosmasligini kerak bo'lsa qisqa eslating.",
-            input=[{"role": "user", "content": f"Vazifa: {task}\nPrompt: {mask_pii(prompt)}\nCase/document context:\n{mask_pii(context_text)[:18000]}"}],
+            input=[{"role": "user", "content": f"Vazifa: {task}\nPrompt: {vault.mask(prompt, known_terms)}\nCase/document context:\n{vault.mask(context_text, known_terms)[:18000]}"}],
         )
-        return ai_output_text(response) or ai_fallback_answer(prompt, context_text)
+        answer = ai_output_text(response)
+        return vault.unmask(answer) if answer else ai_fallback_answer(prompt, context_text)
     except OpenAIError:
         return ai_fallback_answer(prompt, context_text)
 
@@ -7650,7 +7655,7 @@ def ai_document_analysis(payload: AiDocumentAnalysisRequest, authorization: str
     result = heuristic_document_analysis(payload.text)
     settings = get_settings()
     if settings.openai_api_key:
-        answer = run_seller_ai("Hujjatni tahlil qiling va JSON emas, markdown shaklda summary, risks va recommendations bering", payload.text, "document-analysis")
+        answer = run_seller_ai("Hujjatni tahlil qiling va JSON emas, markdown shaklda summary, risks va recommendations bering", payload.text, "document-analysis", name_terms_for(user))
         if answer:
             result["analysis_text"] = answer
     page_count = max(1, (len(payload.text.strip()) + 2499) // 2500)
@@ -7814,7 +7819,7 @@ def admin_e2e_readiness(authorization: str | None = Header(default=None), db: Se
 def ai_assistant(payload: AiAssistantRequest, authorization: str | None = Header(default=None), db: Session = Depends(get_db)) -> dict:
     user = current_user(authorization, db)
     context_text = str(payload.context.get("text") or payload.context.get("case_text") or payload.context.get("document_text") or "")
-    answer = run_seller_ai(payload.prompt, context_text, "seller-assistant")
+    answer = run_seller_ai(payload.prompt, context_text, "seller-assistant", name_terms_for(user))
     add_activity(db, user.id, "ai_assistant", mask_pii(payload.prompt)[:120])
     db.commit()
     return {"answer": answer}
@@ -7832,7 +7837,9 @@ def ai_case_tools(case_id: str, payload: dict, authorization: str | None = Heade
     files = db.scalars(select(MarketplaceRecord).where(MarketplaceRecord.module.in_(["workspace_file", "workspace_file_version"]))).all()
     case_files = [row for row in files if record_payload(row).get("case_id") == case.id or record_payload(row).get("file_id") in set(payload.get("file_version_ids", []) or [])]
     case_text = "\n".join([case.title, case.description, case.next_action, "\n".join(f"{row.title}: {record_payload(row).get('note', '')}" for row in case_files)])
-    masked = mask_pii(case_text)
+    known_terms = name_terms_for(user)
+    pii_vault = PiiVault()
+    masked = pii_vault.mask(case_text, known_terms)
     if tool == "chronology":
         result = heuristic_case_tool("chronology", masked)
     elif tool == "missing_docs":
@@ -7847,9 +7854,12 @@ def ai_case_tools(case_id: str, payload: dict, authorization: str | None = Heade
     else:
         result = heuristic_case_tool("summary", masked)
     if get_settings().openai_api_key and tool != "compare_versions":
-        ai_answer = run_seller_ai(f"Keys bo'yicha {tool} tayyorlang. PII maskalangan. Natijani advokat tahrirlashi mumkin bo'lgan markdown shaklda bering.", masked, f"case-{tool}")
+        ai_answer = run_seller_ai(f"Keys bo'yicha {tool} tayyorlang. PII maskalangan. Natijani advokat tahrirlashi mumkin bo'lgan markdown shaklda bering.", masked, f"case-{tool}", known_terms)
         if ai_answer:
-            result = ai_answer
+            # run_seller_ai only restores the tokens *it* created; the ones
+            # already in `masked` (this handler's own pii_vault, above) are
+            # restored here.
+            result = pii_vault.unmask(ai_answer)
     record = MarketplaceRecord(module="ai_case_tool", record_type=tool, title=f"{tool} {case.case_number}", status="draft", owner_user_id=user.id, payload_json=dump_json({"case_id": case.id, "tool": tool, "result": result, "source_file_ids": [row.id for row in case_files], "editable": True}))
     db.add(record)
     add_activity(db, user.id, "ai_case_tool", tool, "", {"case_id": case.id, "result_id": record.id}, "legal_case", case.id)
diff --git a/tests/test_pii_masking.py b/tests/test_pii_masking.py
new file mode 100644
index 0000000..e02c2d7
--- /dev/null
+++ b/tests/test_pii_masking.py
@@ -0,0 +1,85 @@
+import unittest
+
+from ai_service import PiiVault, mask_pii, name_terms_for
+
+
+# GM v1.1 acceptance test text (T0-17): a client message carrying every
+# category requirement #1 lists — name, PINFL, passport, phone, address,
+# bank card.
+SAMPLE = (
+    "Mening ismim Aziz Karimov, JShShIRim 31234567890123, pasportim AB1234567, "
+    "telefonim +998 90 123 45 67, manzilim Toshkent sh., Chilonzor 5-kvartal "
+    "12-uy, kartam 8600 1234 5678 9012. Qarzdorim pulni qaytarmayapti, nima qilay?"
+)
+
+
+class FakeUser:
+    def __init__(self, name, first_name="", last_name="", middle_name=""):
+        self.name = name
+        self.first_name = first_name
+        self.last_name = last_name
+        self.middle_name = middle_name
+
+
+class MaskPiiTests(unittest.TestCase):
+    def test_phone_pinfl_passport_bank_card_are_masked(self):
+        masked = mask_pii(SAMPLE)
+        self.assertNotIn("31234567890123", masked)
+        self.assertNotIn("AB1234567", masked)
+        self.assertNotIn("+998 90 123 45 67", masked)
+        self.assertNotIn("8600 1234 5678 9012", masked)
+
+    def test_address_keyword_is_masked(self):
+        masked = mask_pii(SAMPLE)
+        # The literal address fragments should not survive verbatim.
+        self.assertNotIn("Chilonzor 5-kvartal 12-uy", masked)
+
+    def test_no_known_name_leaves_the_name_unmasked(self):
+        # mask_pii with no known_terms has no way to find a free-text name —
+        # this documents the limitation rather than a false expectation.
+        masked = mask_pii(SAMPLE)
+        self.assertIn("Aziz Karimov", masked)
+
+    def test_known_name_is_masked(self):
+        vault = PiiVault()
+        masked = vault.mask(SAMPLE, ["Aziz Karimov"])
+        self.assertNotIn("Aziz Karimov", masked)
+
+    def test_name_terms_for_user(self):
+        user = FakeUser(name="Aziz Karimov", first_name="Aziz", last_name="Karimov")
+        terms = name_terms_for(user)
+        self.assertIn("Aziz Karimov", terms)
+        self.assertIn("Aziz", terms)
+        self.assertIn("Karimov", terms)
+
+    def test_name_terms_for_guest_is_empty(self):
+        self.assertEqual(name_terms_for(None), [])
+
+    def test_round_trip_restores_the_original_value_in_a_reply(self):
+        # Requirement #2: once the (masked) request comes back from the
+        # external AI, the original values must be restored for the user —
+        # this was entirely unimplemented before this fix (fixed placeholder
+        # strings had no way back to the real value).
+        vault = PiiVault()
+        masked = vault.mask(SAMPLE, ["Aziz Karimov"])
+        name_token = next(tok for tok in masked.split() if tok.startswith("[NAME_"))
+        # Strip any trailing punctuation the split() may have caught.
+        name_token = name_token[: name_token.index("]") + 1]
+        simulated_ai_reply = f"Salom {name_token}! Sizning holatingizda ..."
+        restored = vault.unmask(simulated_ai_reply)
+        self.assertIn("Aziz Karimov", restored)
+        self.assertNotIn("[NAME_", restored)
+
+    def test_repeated_value_reuses_the_same_token(self):
+        # The same PII value appearing twice must map to one token, not two —
+        # otherwise the reply could restore the second occurrence with the
+        # wrong original value.
+        vault = PiiVault()
+        text = "Tel: +998 90 123 45 67. Yana bir bor: +998 90 123 45 67."
+        masked = vault.mask(text)
+        tokens = [w for w in masked.replace(".", " ").split() if w.startswith("[PHONE_")]
+        self.assertEqual(len(set(tokens)), 1)
+
+
+if __name__ == "__main__":
+    unittest.main()
